class Solution(object):

    def generateParenthesis(self, n):
        """
        :type n: int
        :rtype: List[str]
        """
        prev_level_brackets = ["()"] # 1
        for i in range:
            curr_level_brackets = []
            
            for prev_bracket in prev_level_brackets:
                # суммируем
                
            # вкладываем